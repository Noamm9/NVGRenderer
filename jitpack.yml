# Tests are skipped on JitPack: ResourceLoaderTest hits a live HTTP endpoint
# and is not deterministic in a CI environment.
jdk:
  - openjdk21
before_install:
  - sdk install java 25.0.3-tem
install:
  - JAVA_HOME=$(sdk home java 25.0.3-tem) ./gradlew build publishToMavenLocal -x test --no-daemon

